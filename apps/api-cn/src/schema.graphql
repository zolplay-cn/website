# ------------------------------------------------------
# THIS FILE WAS AUTOMATICALLY GENERATED (DO NOT MODIFY)
# ------------------------------------------------------

type Auth {
  """JWT access token"""
  accessToken: JWT!

  """JWT refresh token"""
  refreshToken: JWT!
  user: User!
}

input CreatePageCommentInput {
  content: String!
  route: String!
}

"""
A date-time string at UTC, such as 2019-12-03T09:54:33Z, compliant with the date-time format.
"""
scalar DateTime

"""
A field whose value is a JSON Web Token (JWT): https://jwt.io/introduction.
"""
scalar JWT

input LoginInput {
  phone: String!
  verificationCode: String!
}

type Mutation {
  createPageComments(data: CreatePageCommentInput!): PageComment!
  login(data: LoginInput!): Auth!
  refreshToken(token: JWT!): Token!
  sendSmsCode(data: SmsInput!): Boolean!
  signup(data: SignupInput!): Auth!
  updateUser(data: UpdateUserInput!): User!
}

type PageComment {
  content: String!

  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  id: ID!
  route: String!

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
  user: User!
}

type Query {
  me: User!
  post(pageCommentId: String!): PageComment!
  version: String!
}

input SignupInput {
  name: String!
  phone: String!
  verificationCode: String!
}

input SmsInput {
  phone: String!
  type: String!
}

type Subscription {
  postCreated: PageComment!
}

type Token {
  """JWT access token"""
  accessToken: JWT!

  """JWT refresh token"""
  refreshToken: JWT!
}

input UpdateUserInput {
  name: String
}

type User {
  """Identifies the date and time when the object was created."""
  createdAt: DateTime!
  email: String
  id: ID!
  name: String
  pageComments: [PageComment!]!
  phone: String
  teamLabel: String

  """Identifies the date and time when the object was last updated."""
  updatedAt: DateTime!
}
